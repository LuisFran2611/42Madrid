#ifndef REPLACE_HPP
#define REPLACE_HPP

#include <string>

class Replace {
public:
    static std::string replaceAll(const std::string& line, const std::string& s1, const std::string& s2);
};

#endif